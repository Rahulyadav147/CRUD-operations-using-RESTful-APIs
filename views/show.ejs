<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Post Details</title>
  <!-- Tailwind CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-8">
  <div class="max-w-3xl mx-auto bg-white p-8 rounded-lg shadow-md border border-gray-300">
    <h1 class="text-3xl font-bold text-gray-900 mb-6">Post Details</h1>
    
    <div class="post mb-6">
      <h3 class="text-xl font-semibold text-gray-800 mb-2">Post ID: <%= post.id %></h3>
      <h4 class="text-lg font-semibold text-blue-700 mb-4">@<%= post.username %></h4>
      <p class="text-gray-800 text-lg"><%= post.content %></p>
    </div>

    <a 
      href="3001/posts" 
      class="text-blue-600 hover:text-blue-700 font-semibold transition duration-300"
    >
      Back to Posts
    </a>
  </div>
</body>
</html>
